<%= simple_form_for([:dashboard, @snf_setting]) do |f| %>

  <%= f.input :gstin_number %>
  <%= f.input :whats_new, class: 'tinymice' %>
  <%= f.input :faqs, class: 'tinymice' %>
  <%= f.submit 'UPDATE SNF SETTINGS', class: "btn btn-default" %>

<% end %>
<script>
  tinymce.init({
    selector: 'textarea',
    height: 500,
    menubar: true,
    forced_root_block : "",
     table_class_list: [{title: 'Bootstrap', value: 'table table-striped table-bordered table-hover'}],
    plugins: [
      'advlist autolink lists link image charmap print preview anchor',
      'searchreplace visualblocks code fullscreen',
      'insertdatetime media table contextmenu paste code'
    ],
    toolbar: 'undo redo | insert | styleselect | bold italic | alignleft aligncenter alignright alignjustify | bullist numlist outdent indent | link image',
    content_css: [
      '//fonts.googleapis.com/css?family=Lato:300,300i,400,400i',
      '//www.tinymce.com/css/codepen.min.css']
  });
</script>